{% extends 'base.html.twig' %}

{% block title %}Edit Calificacion{% endblock %}

{% block body %}
    <h1>Edit Calificacion</h1>

    {{ include('calificacion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_calificacion_index') }}">back to list</a>

    {{ include('calificacion/_delete_form.html.twig') }}
{% endblock %}
