{% extends 'base.html.twig' %}

{% block title %}Calificacion{% endblock %}

{% block body %}
    <h1>Calificacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calificacion.id }}</td>
            </tr>
            <tr>
                <th>Calificacion</th>
                <td>{{ calificacion.calificacion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_calificacion_index') }}">back to list</a>

    <a href="{{ path('app_calificacion_edit', {'id': calificacion.id}) }}">edit</a>

    {{ include('calificacion/_delete_form.html.twig') }}
{% endblock %}
