<form method="post" action="{{ path('app_calificacion_delete', {'id': calificacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calificacion.id) }}">
    <button class="btn">Delete</button>
</form>
