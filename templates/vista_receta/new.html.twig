{% extends 'basehome.html.twig' %}

{% block title %}Crear Receta{% endblock %}

{% block body %}
<div class="form-container">
    <h1>Crear nueva Receta</h1>

    {{ form_start(form) }}
        {{ form_row(form.nombre, {'attr': {'class': 'formulario-input'}}) }}
        {{ form_row(form.png, {'attr': {'class': 'formulario-input'}}) }}
        {{ form_row(form.descripcion, {'attr': {'class': 'formulario-input'}}) }}
        {{ form_row(form.tiempoprep, {'attr': {'class': 'formulario-input'}}) }}
        {{ form_row(form.porciones, {'attr': {'class': 'formulario-input'}}) }}
        {{ form_row(form.dificultad, {'attr': {'class': 'formulario-input'}}) }}

        <div class="form-group">
            {{ form_label(form.categoria) }}
            <div class="categoria-container">
                <div class="categoria-grid">
                    {% for choice in form.categoria %}
                        <div class="categoria-item">
                            <div class="checkbox-container">
                                {{ form_widget(choice) }}
                                {{ form_label(choice) }}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
        

        <div class="form-group">
            {{ form_label(form.ingredientes) }}
            <div class="categoria-container">
                <div class="categoria-grid">
                    {% for choice in form.ingredientes %}
                        <div class="categoria-item">
                            <div class="checkbox-container">
                            {{ form_widget(choice) }}
                            {{ form_label(choice) }}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        {{ form_row(form.usuario, {'attr': {'class': 'formulario-input'}}) }}

        <button type="submit" class="btn">{{ button_label|default('siguiente') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_perfiluser') }}" class="btn-back">Volver</a>
</div>

<div class="instrucciones" id="instrucciones">
    <button id="toggle-instrucciones">−</button>
    <h2>Instrucciones:</h2>
    <ol>
        <li>Nombre su receta.</li>
        <li>Luego, si lo desea puede subin una imagen de su receta.</li>
        <li>Coloque una breve descripción de la misma, Aqui puede ir la lista de ingredientes y medidas.</li>
        <li>Indique el tiempo de preparacion.</li>
        <li>Elija el nivel de dificultad que considere apropiado.</li>
        <li>Seleccione una o varias categorias a las  que pertenece su receta.</li>
        <li>Seleccione los ingredientes que lleve su receta.</li>
        <li>Una vez revisado todo, elija la opcion "siguiente" y a continuacion especificaremos los pasos para realizar su receta.</li>
    </ol>
</div>
{% endblock %}
