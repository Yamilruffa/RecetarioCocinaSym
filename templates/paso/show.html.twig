{% extends 'base.html.twig' %}

{% block title %}Paso{% endblock %}

{% block body %}
    <h1>Paso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ paso.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ paso.descripcion }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ paso.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paso_index') }}">back to list</a>

    <a href="{{ path('app_paso_edit', {'id': paso.id}) }}">edit</a>

    {{ include('paso/_delete_form.html.twig') }}
{% endblock %}
