{% extends 'basehome.html.twig' %}

{% block body %}
<main class="homepage">
    <section class="titulo">
        <div class="logoyh1">
            <img src="{{ asset('img/logo.png') }}" alt="Logo" class="h1logo">
            <h1>Bienvenido a Recetas RosaBri</h1>
        </div>
        <p>Descubre, comparte y disfruta de recetas hogareñas.</p>
        <a href="{{ path('app_vista_receta_todas') }}" class="btn">Explorar Recetas</a>
    </section>
</main>

<!-- Contenedor Flex con dos columnas: "Recetas Destacadas" y el Carrusel -->
<div class="contenedor-flex">
    <div class="destacado">
        <h2>Recetas Destacadas</h2>
        <p>Descubre las recetas más populares de nuestra comunidad.</p>
    </div>

    <div class="carousel-container">
        <div class="carousel">
            <div class="carousel-track-container">
                <ul class="carousel-track">
                    {% for receta in recetas %}
                    <li class="carousel-slide {% if loop.first %}current-slide{% endif %}">
                        <a href="{{ path('app_vista_receta_show', {'id': receta.id}) }}">
                            <img src="{{ asset(receta.png|slice(1)) }}" alt="{{ receta.nombre }}" class="carousel-image" loading="lazy">
                        </a>
                        <h3 class="carousel-title">{{ receta.nombre }}</h3>
                    </li>
                    {% endfor %}
                </ul>
            </div>

            <button class="carousel-button carousel-button-left" aria-label="Anterior">❮</button>
            <button class="carousel-button carousel-button-right" aria-label="Siguiente">❯</button>
            <div class="carousel-indicators">
                {% for receta in recetas %}
                <button class="indicator {% if loop.first %}active{% endif %}" data-slide="{{ loop.index0 }}"></button>
                {% endfor %}
            </div>
        </div>
    </div>
</div>

<!-- Sección "¿No sabes qué cocinar hoy?" con dos div separados -->
<div class="receta-random-container">
    <div class="receta-random-titulo">
        <h2>¿No sabes qué cocinar hoy?</h2>
    </div>
    <div class="receta-random-contenido">
        <h3>Prueba la receta del día:</h3>
        {% if recetaRandom %}
            <a href="{{ path('app_vista_receta_show', {'id': recetaRandom.id}) }}">
                <img src="{{ asset(recetaRandom.png|slice(1)) }}" alt="{{ recetaRandom.nombre }}" class="receta-random-img" loading="lazy">
            </a>
        {% else %}
            <p>No hay recetas disponibles en este momento. ¡Vuelve pronto!</p>
        {% endif %}
    </div>
</div>

<div id="sobre-nosotros" class="sobre-nosotros">
    <div class="sobre-nosotros-contenido">
        <h2>Sobre Nosotros</h2>
        <p>Recetas RosaBri es una plataforma online donde podés compartir tus recetas favoritas y descubrir nuevas recetas de cocina. ¡Únite a nuestra comunidad!</p>
    </div>
</div>

<footer class="footer">
    <p>Recetas RosaBri 2024</p>
    <div class="footer-links">
        <div class="footer-column">
            <a href="#">Contacto</a>
            <a href="#">Términos y Condiciones</a>
        </div>
        <div class="footer-column">
            <a href="#">Política de Privacidad</a>
            <a href="#">Cookies</a>
        </div>
    </div>
</footer>

{% endblock %}
