{% extends 'basehome.html.twig' %}

{% block title %}Agregar Pasos{% endblock %}

{% block body %}
<div class="layout">
    <div class="contenedor-instrucciones">
        <div class="instrucciones" id="instrucciones">
            <button id="toggle-instrucciones">−</button>
            <h2>Instrucciones:</h2>
            <ol>
                <li>Ahora explicaremos los pasos en orden numérico.</li>
                <li>Primero coloque el nombre que defina el paso.</li>
                <li>Luego coloque la descripción del paso.</li>
                <li>Indique cuál es el número de este paso.</li>
                <li>Una vez finalizado el formulario, si desea agregar otro paso, elija la opción "Agregar otro paso", de lo contrario seleccione "Finalizar".</li>
            </ol>
        </div>
    </div>

    <div class="main-content">
        <div id="contenedor-pasos">
            <h1>Agregar Pasos a la Receta</h1>
            <form id="paso-form" method="POST" action="{{ path('app_vista_pasos_new', {'id': receta.id}) }}">
                {{ form_start(form) }}
                {{ form_row(form.nombre, {'attr': {'class': 'formulario-input'}}) }}
                {{ form_row(form.descripcion, {'attr': {'class': 'formulario-input'}}) }}
                {{ form_row(form.numero, {'attr': {'class': 'formulario-input'}}) }}
                {{ form_row(form.receta, {'attr': {'class': 'formulario-input'}}) }}
                <button type="submit" name="action" value="add" class="btn">Agregar otro paso</button>
                <button type="submit" name="action" value="finalizar" class="btn-back">Finalizar</button>
                {{ form_end(form) }}
            </form>
        </div>

        <div id="table-container">
            <h2>Pasos agregados:</h2>
            <table class="pasos-table">
                <thead>
                    <tr>
                        <th>Número</th>
                        <th>Nombre</th>
                        <th>Descripción</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {% for paso in pasos %}
                        <tr>
                            <td>{{ paso.numero }}</td>
                            <td>{{ paso.nombre }}</td>
                            <td>{{ paso.descripcion }}</td>
                            <td>
                                <a href="{{ path('app_vista_pasos_edit', {'id': paso.id}) }}" class="btn-edit">Editar</a>

                                <!-- Botón para abrir el modal de eliminación -->
                                <button type="button" class="btn-delete" onclick="openDeleteModal('deleteModalPaso-{{ paso.id }}')">Eliminar</button>

                                <!-- Modal de confirmación de eliminación -->
                                <div id="deleteModalPaso-{{ paso.id }}" class="modal" style="display: none;">
                                    <div class="modal-content">
                                        <span class="close" onclick="closeDeleteModal('deleteModalPaso-{{ paso.id }}')">&times;</span>
                                        <h3>¿Estás seguro de que deseas eliminar este paso?</h3>
                                        <form id="deletePasoForm-{{ paso.id }}" action="{{ path('app_vista_pasos_delete', {'id': paso.id}) }}" method="POST">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paso.id) }}">
                                            <button type="submit" class="btn btn-danger">Eliminar</button>
                                            <button type="button" class="btn btn-secondary cancelBtn" onclick="closeDeleteModal('deleteModalPaso-{{ paso.id }}')">Cancelar</button>
                                        </form>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4">No hay pasos agregados.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>
    function openDeleteModal(id) {
        document.getElementById(id).style.display = 'block';
    }

    function closeDeleteModal(id) {
        document.getElementById(id).style.display = 'none';
    }
</script>

{% endblock %}
